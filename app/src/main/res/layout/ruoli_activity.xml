<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ruoli_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/sfondo">

    <include android:id="@+id/profile_btn_roles"
        layout="@layout/profile_icon_layout"/>

    <include android:id="@+id/back_btn_roles"
        layout="@layout/back_btn_layout"/>

    <include android:id="@+id/profile_sidebar_roles"
        layout="@layout/profile_sidebar"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/lista_ruoli"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintWidth_percent="0.75"
        app:layout_constraintHeight_percent="0.58"
        android:layout_marginTop="80dp"
        android:layout_marginHorizontal="40dp"
        android:padding="10dp"
        android:background="@drawable/white_border_grey_bg"
        android:columnCount="3"
        android:rowCount="6"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>